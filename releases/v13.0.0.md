# Savant Server v13.0.0

Released from `build-info.json`.

## Build Info

- Version: `13.0.0`
- Branch: `main`
- Commit: `8af684a`
- Built at: `2026-07-19T03:10:52Z`

## Code Intelligence Runtime

- CodeGraph `1.4.1` runs inside the server image on pinned Node `22.17.0`.
- The private bridge uses an in-container Unix socket and is supervised with the API and MCP processes.
- Container health requires both the bridge ping and Flask/database readiness.

## Version and Health Endpoints

- `GET /version`
- `GET /api/version`
- `GET /health/live`
- `GET /health/ready`
