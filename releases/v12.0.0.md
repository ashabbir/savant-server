# Savant Server v12.0.0

Released from `build-info.json`.

## Build Info

- Version: `12.0.0`
- Branch: `main`
- Commit: `09edf6c`
- Built at: `2026-04-27T17:08:00Z`

## Version Endpoints

- `GET /version`
- `GET /api/version`

Both endpoints return:

- `version`
- `branch`
- `commit`
- `built_at`

## Health Endpoints

- `GET /health/live`
- `GET /health/ready`

These also include the build version in their JSON payload.
